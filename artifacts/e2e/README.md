# Browser evidence status

`report.json` records the latest attempt, `9b10c9328d`, which was interrupted after six completed stages when the tool context reset and the Mac was found locked. It is not a passing end-to-end run.

No exports were obtained in that attempt. The adjacent result JSON/CSV/SVG, native archive and verification report belong to the earlier completed run `3e69dc6d08` (native election `djz6Qgu7Yr2wSY`, PR #47). They are historical evidence, not verification of the current change. A fresh complete Browser run is required before merging the trustee-intake change.
