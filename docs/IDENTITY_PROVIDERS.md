# Identity provider contracts

Checked against official public sources on 2026-09-06. Only the synthetic A/B contract is executable. No production endpoint, payload, certificate parser or callback has been invented. `IdentityProvider<Request, Assertion>` is an internal typed boundary; it does not imply that production providers share the mock wire format. Every production adapter returns `AwaitingProviderContract` and rejects authentication without a network call. Production configuration rejects the mock adapter before transport.

| Provider | Stable subject / RNOKPP owner | Optional attributes | Freshness and proof validation | Access / actual state |
|---|---|---|---|---|
| Mock A / B | Same `TEST-PERSON` fixture across both; explicit synthetic owner flag | Explicit fixture age and citizenship | Signed Ed25519 assertion, distinct pretrusted key, request challenge, subject/provider match; maximum age/lifetime 120 seconds | Separate local processes and keys; `SyntheticImplemented` |
| Дія.Підпис | The exact stable-subject and RNOKPP-ownership contract for this application has not been supplied | Neither age nor citizenship is claimed by this integration | Production challenge/callback, trust chain and signature validation remain unimplemented | Technical access and agreed contract required; `AwaitingProviderContract` |
| BankID НБУ | Deduplication must use approved verified personal attributes, not a bank account ID. An allowed dataset containing RNOKPP is required | Age requires an approved birth-date attribute; citizenship is not inferred | The current specification must govern session, transport, cryptographic response and freshness; no adapter execution is claimed | Admission, agreement, approved datasets, subscriber node and integration testing; `AwaitingProviderContract` |
| КЕП | The physical-person certificate profile and cross-provider matching rule must be established | No automatic age/citizenship inference | Signed fresh challenge, trusted qualified provider, certificate validity/revocation and a verified implementation are required; none is simulated as production validation | Profile, verifier and trust configuration required; `AwaitingProviderContract` |

Дія documents signature-based authorization and technical integration steps, but its public scenario page does not establish this application's machine contract or returned eligibility attributes. AntiBot is described as authorization without receiving personal data; it is not accepted as a source of a stable person identifier or citizenship. [Дія.Підпис](https://integration.diia.gov.ua/signature.html), [Дія AntiBot](https://integration.diia.gov.ua/antibot.html).

The NBU describes subscriber admission, an agreement and permission for specific standardized datasets. Its official documents page links the interaction specification. A page describing those requirements is not evidence that this project is admitted or integrated. The documents page's direct retrieval returned HTTP 403 during this check; the official indexed page and connection instructions were readable. [NBU connection procedure](https://bank.gov.ua/ua/bank-id-nbu/connect), [NBU documents](https://bank.gov.ua/ua/bank-id-nbu/documents).

The CZO publishes the list of qualified trust service providers and their qualified services. That list is a trust input; it is not, by itself, a signature verifier or proof of a person's citizenship. The indexed official page was available; direct retrieval timed out during this check. [CZO trusted list](https://www.czo.gov.ua/trustedlist).

## Dependency and outage policy

A and B run as two independently controlled local mock processes with different signing keys, but their host and fixture definition are shared. This simulates provider failures, not organizational independence. Banks selected through BankID share the NBU central node. A concrete QES provider and Дія.Підпис may share trust or revocation dependencies; independence cannot be claimed without the actual deployment contracts. An unintegrated production adapter is not an available recovery channel.

The UI shows observed A/B availability and refreshes it only on entry or an explicit user action. An unreachable transport is labeled as an unreceived response; invalid status data is rejected. No automatic provider switch or weaker identification exists. A failed return keeps the same attempt in component memory, with at most three explicit deliveries. Selecting another provider is explicit; the backend deduplicates the same verified person. Late/concurrent returns for one completed attempt return the same sealed session token. A discarded component does not sign the user in after its response arrives. A reload loses the pending page state and requires identification again; it does not reset the voting right.

Issued credentials remain valid until the fixed close under the predeclared frozen-registry policy. An outage does not extend a poll. Revoking an identity prevents subsequent sessions/claims but does not erase an already exported credential from the native frozen registry. This residual limit remains open in the revocation work; it is not silently relaxed during an incident.

## Verification

`pnpm exec vitest run tests/providers.test.ts` checks real standard signatures, replay/request/freshness binding, unauthorized signatures, malformed status, disabled contracts and rejection of production mocks. It uses simulated HTTP responses and does not claim a real production integration. `tests/services.test.ts` independently exercises the two live mock processes, concurrent A/B deduplication, delayed return, both outages, conflicting attributes, same-credential recovery and native voting. Its expiry test waits until the private mock control confirms an in-flight response, then expires exactly that attempt. The pre-fix service incorrectly returned HTTP 200; the fixed transaction rechecks expiry before granting a session. Browser assertions are recorded separately by the existing in-app Browser runner.
