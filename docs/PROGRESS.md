# Actual progress

## 2026-09-06 — initial reproduction

The selected directory was empty, not a Git repository, with no application, tests, migrations or local instructions. There is no existing user behavior to preserve. Both user requirement attachments have been read. Docker is available. GitHub connector and authenticated CLI both identify `Lontkovsky`; repository name availability was checked.

Current work: repository bootstrap and requirements backlog, followed by the mandatory real-cryptography vertical slice. No implementation test, external audit, deployment, release, or production readiness is claimed yet.

## Reference slice evidence

- Public repository: https://github.com/Lontkovsky/vidkrytyi-holos. Public board: https://github.com/users/Lontkovsky/projects/5. The board returned HTTP 200 without credentials with the actual project title. Private vulnerability reporting was enabled and read back as `enabled: true`. Bootstrap main had no branch protection; no existing rules were bypassed.
- 11 epics and 30 vertical issues created, with separate Development release and Production Readiness milestones. `docs/backlog.json` contains actual issue URLs.
- Pinned Belenios 3.3.0 native executable and its original browser library compiled successfully in Docker. `belenios-tool --version` returned `3.3.0 (3.3.0)`.
- The official unmodified `tests/tool/demo-threshold.sh` completed successfully: setup, ballots, upstream revotes, finality, threshold plus mandatory decryption, result and reference verification. This is an upstream compatibility check; our final-vote API separately forbids revotes.
- `sh scripts/test-reference.sh`: twelve actual checks passed, with five synthetic encrypted votes producing `[[3,1,1]]`. These include corrupt ciphertext, corrupt zero-knowledge proof and a forged result in a structurally/hash-consistent archive. The report and safe public archive are in `artifacts/reference/`. The initial test expected string counts; upstream returned numeric counts, so the test contract was corrected without changing/normalizing the reference output.
- This remains an incomplete development product: provider/application APIs, database, browser journey, full A01–A11 guards, observer, release and production work are not yet complete.

## Continue from here

Read the current GitHub issues/board and working tree before proceeding. Do not create duplicate repositories, projects or issues. Preserve uncommitted changes. Record each verified command with its actual outcome and source revision.

## First application vertical slice

- Reference PR [#42](https://github.com/Lontkovsky/vidkrytyi-holos/pull/42) merged as `69b21b4f5f03a9f2c88729cc9d3798af4b307909` after the real `reference` CI job passed. This was self-review, not external approval or audit.
- Local bootstrap now starts PostgreSQL 18.4 with three separate databases/users, two independently signed synthetic provider processes, identity, ballot and management APIs. Node 24.20.0, pnpm 10.33.0 and dependencies are pinned.
- Reproduced and fixed a setup integration defect: Zod reordered JSON object keys, making a stringified comparison reject an otherwise equal native election. Structural equality now compares all values without changing the reference format. Retrying the interrupted provisioning reused the same registry, credentials and trustee keys.
- Reproduced and fixed browser packaging: the raw compiler output requires the official Belenios static wrapper and `libsodium.wasm`. The Docker build now builds the upstream `src/web/static/belenios_jslib.js` target unchanged and distributes upstream licenses.
- `pnpm test --reporter=default --reporter=json --outputFile=artifacts/services/tests.json`: six tests passed in 47.44 seconds. The integration test covers concurrent A/B deduplication, outages/late callback replay, conflicting attributes/revoked sessions, optional eligibility, same-right recovery, five concurrent identical deliveries, changed final vote rejection, immutable context, role checks, early closing rejection, fixed deadline, signed-final-set trustee guards, insufficient quorum and a real 1/1/1 threshold tally. `artifacts/services/` contains safe public evidence only. Tests delete only polls they created; the seed scenario remains.
- `pnpm types`, `pnpm build`, `pnpm smoke`, and `pnpm seed` completed. `pnpm audit --json` reported zero known advisories for the installed dependency graph; this is not a security audit.
- Existing in-app Browser, same tab/session: opened the seed question, confirmed a synthetic person, selected and explicitly confirmed a choice, invoked original browser crypto, obtained a signed receipt and verified tracker inclusion. Desktop screenshot and a 390×844 mobile screenshot were inspected; measured document width and scroll width both equal 390. No browser storage/session persistence was introduced. This is direct browser evidence, not yet a reusable full E2E suite.
- UI includes catalog, question/context/sources/policy, draft editor, moderation queue, appeals, voting, tracker, results and trust/help routes. Only the participation/receipt journey has browser evidence at this checkpoint; creation/moderation/publication UI and negative browser paths still require verification.
- The product remains incomplete. Observer/split-view checks, receipt recovery journal/backup verification, full A01–A11 controls, exports/share cards, provider contract matrices, OpenAPI, resource/security scans, reproducibility/client monitor, production readiness documentation, complete E2E and release packaging remain open. No release or public hosted demo has been created.

- Additional browser negative path: signed out, re-identified the same synthetic person through provider B, submitted a different encrypted answer, and observed the explicit already-final-voted rejection. No automatic revote or new right was created. The temporary mobile viewport override was reset.

## Application CI and browser contracts

- Application PR [#43](https://github.com/Lontkovsky/vidkrytyi-holos/pull/43) merged as `468172ead0056fa84dc54a9efd489d1514b40096` after the actual [reference CI job](https://github.com/Lontkovsky/vidkrytyi-holos/actions/runs/34047444871/job/101524925430) passed in 3m24s. Versioned eligibility issue #16 closed; remaining criteria stay open. This was self-review, not independent audit.
- Reusable E2E now runs through the existing in-app Browser binding only. A coordinator records ordered UI assertions and compares initial/final source digests. It fails on a failed/incomplete/interrupted run. `docs/BROWSER_TESTING.md` contains the exact procedure.
- Reproduced a catalog hit-area defect: a wrapping inline heading link's rectangle center landed outside the link's inline rectangles. Making the heading link a block fixes the real click target. The moderator now gets an accessible explicit completion message. Export filenames include the native election ID.
- Browser run `b734911ff5` passed creation/edit/review, three encrypted votes, rejection through the other provider, fixed close and native 1/1/1 tally. It failed at the download-event assertion. The actual downloaded result file existed and matched the expected question and counts. That run is not reported as passed. The corrected test reads the new completed files saved by the browser and checks their contents.
- Corrected browser run `2dc9e08822` completed all 10 stages and 22 assertions with CLI exit 0 at 17:45:47 UTC. Initial/final source digest: `4b662e890996011082b51c826baed1b5b7b7679960054172050734992394edb3`. `artifacts/e2e/report.json`, `result.json` and the browser-downloaded `election.bel` preserve safe evidence. `pnpm verify artifacts/e2e/election.bel` independently passed native proofs and returned `[[1,1,1]]`, three ballots and closed status with networking disabled. `pnpm types`, `pnpm build`, public-artifact scan and diff whitespace checks passed. The full published-page desktop screenshot was visually inspected. Complete keyboard/offline/error coverage remains open in #23; this is not a completed release.
- Browser PR [#44](https://github.com/Lontkovsky/vidkrytyi-holos/pull/44) merged as `75cc0ea` after [actual CI success](https://github.com/Lontkovsky/vidkrytyi-holos/actions/runs/34049740626/job/101531060694), 3m28s. No external review is implied.

## Provider contracts and outage recovery

- Before changes, the Browser displayed only `PROVIDER_UNAVAILABLE`; it ignored server availability and every retry started another attempt. The new UI preserves the attempt in component memory, limits explicit delivery to three, shows observed A/B availability and never selects B automatically. Capability disclosures keep Дія.Підпис, BankID НБУ and КЕП visibly AwaitingProviderContract.
- `IdentityProvider<Request, Assertion>` now supplies a real signed mock adapter and three disabled production boundaries. Official sources and unverified contract details are recorded in `docs/IDENTITY_PROVIDERS.md`. No unknown endpoint or production proof validator is fabricated.
- Reproduced a return-expiry race with an observed in-flight mock response: expire exactly that attempt while the provider is responding. Before the fix the API returned 200; it must return 410. The transaction now rechecks expiry before granting a session. `pnpm exec vitest run tests/services.test.ts -t 'expires pending'` failed before and passed after this change.
- Seven adapter contract tests passed, along with types and build. Full live-service and expanded Browser results are pending at this checkpoint; no issue is closed on implementation alone.
- A second deterministic test held the person's advisory lock and expired the attempt while the login transaction waited. PostgreSQL transaction-time `now()` still admitted it (HTTP 200). The post-lock check now uses `clock_timestamp()`; both expiry windows are separate regression cases. Credential recovery after the immutable poll deadline is also asserted as HTTP 403.
- Browser run `51f7800726` passed the complete voting/export journey but failed at a role-locator evaluation timeout in the new keyboard assertion. The next DOM snapshot showed Mock B checked; a documented locator intersection with `:checked` returned 1. The failing role-evaluation assertion was replaced; the failed run is not a passing release check.
- Final local service run: 15 tests passed in 49.27 seconds, with both expiry races and post-close credential denial included. Safe, path-relative report: `artifacts/services/tests.json`. Types, build, dependency-lock review, public-artifact scan and whitespace checks passed; no dependency was added.
- Expanded Browser run `aae0e3836f`: all 14 stages / 33 assertions passed, CLI exit 0, unchanged source digest `4c0cc490512496851c8d18094f6be3de021197294a1bf076451dc6e4c88f3f4f`, finished 18:23:46 UTC. It verifies keyboard capability disclosures, explicit disabled production states, failure/recovery of the same attempt, the three-delivery limit, explicit switch to B, both outages and restored status. The fixed poll still publishes 1/1/1; the browser-downloaded native archive independently verifies without network/private storage. Evidence: `artifacts/e2e/`.
- Mobile provider page visually inspected at 390×844: document and scroll width both 390, readable controls/disclosures/warnings. Temporary viewport override reset; existing tab retained. Complete offline-ballot, recovery/revocation, observer, public export/share and remaining A01–A11/release work remain open.
- Implementation submitted as [PR #45](https://github.com/Lontkovsky/vidkrytyi-holos/pull/45); CI and merge pending at this checkpoint.
- PR #45 subsequently merged as `a18b607` after [actual CI success](https://github.com/Lontkovsky/vidkrytyi-holos/actions/runs/34051937970/job/101537027763), 3m36s. Development provider issues #17 and #18 closed; production integrations remain disabled pending real contracts.

## Unified public result exports

- Audited #26 and reproduced the existing anonymous published page: JSON and audit downloads existed; CSV and sharing were absent. The stored result already originated from native tally and supplied the UI. The change extends this contract, with no new counter or identity-data join.
- Added public CSV/SVG routes behind the same Published gate, exact result metadata in the UI, bounded share-card rendering, and independent Python parsers for downloaded files. The schema now enforces count sum, positive version, fixed warning, valid time bounds and exportable question characters. Invalid characters are rejected, never stripped. CSV encoding and its spreadsheet interpretation boundary are documented in `docs/PUBLIC_RESULTS.md`.
- Five export tests passed, including 100 deterministic Unicode/CSV round trips, formula text, XML markup escaping, exact SVG metadata, zero/unanimous rendering and invalid-contract rejection. Initial full service run hit connection-refused during an observed Node watch restart; that run failed. All five services then passed the readiness probe before rerunning. Full service and Browser evidence is pending at this checkpoint.
- Final local run: 21 tests passed in 60.59s. Actual native service tests reject all four public formats before publication and after invalidation, and close a real empty ballot box into ResultsSuppressed without exporting counts. Published CSV/SVG API responses match the shared serializer and include attachment headers. Types and build passed; no dependencies or lockfile changed.
- Browser run `db15d4b231` passed all 14 stages / 36 assertions with CLI exit 0 at 18:58:52 UTC. Initial/final source digest: `62c27332ff035bd2bf06c117890f430794ac99925ebef160d78b5edd7bd38e88`. It downloads JSON, CSV, SVG and audit anonymously, compares every contract field with independent standard-library parsers, and checks keyboard preview controls. Safe evidence is in `artifacts/e2e/`; the unchanged downloaded native archive passed offline Belenios verification with three ballots and `[[1,1,1]]`.
- Desktop share-card and mobile published-page screenshots were inspected. At 390px, document/scroll widths both equal 390; the full textual result remains readable above the scaled image preview. Viewport reset and existing tab retained. After sign-out, the application returned to the catalog as designed; a subsequent manual preview check first had to reopen the question. The automated suite had already asserted that navigation explicitly.
- Export work is locally verified; PR/CI is the next gate. Observer, anti-rollback restoration, remaining attack controls and complete release packaging remain open. No external audit or production readiness is claimed.
- Export [PR #46](https://github.com/Lontkovsky/vidkrytyi-holos/pull/46) merged as `ddd9472` after [actual reference CI success](https://github.com/Lontkovsky/vidkrytyi-holos/actions/runs/34053660385/job/101541578107), 3m44s. Issue #26 closed. The CSV bytes were preserved unchanged with a scoped CRLF Git attribute; staged-byte equality and whitespace checks passed.

## Independent public observer

- Audited #27, the public result/checkpoint/catalogue flow and existing native verifier. Reproduction: `pnpm run observer` returned Missing script. The existing `verifyChain` accepted signed receipt additions after a Closed checkpoint because it only checked monotonic growth; a new test reproduces that behavior. The observer additionally freezes the closed receipt set and validates state transitions. Existing native cryptographic formats are unchanged.
- Implemented a separate GET-only client, strict pretrusted configuration, signed-history persistence/comparison and `audit:export`. It checks public UI metadata against the pinned manifest and performs real native verification before labeling a published observation ReferenceVerified. Stored state excludes full question/archive content. Missing history, forks, lost acknowledgements and unavailable/removed data fail without overwriting accepted history.
- Focused observer suite initially passed eight cases, then nine after verified export was added. Live `observer init`, `observer check` and `audit:export` succeeded for browser poll `bWR6QtTPMZawJd`, with three checkpoints and native archive hash `08d707dbd979b35844392734ec9a31e067cb33f92619d47190156679ad0c271d`. Trust came from already retained synthetic public artifacts, not an independently audited ceremony. No identity secret or private backend was accessed.
- A full run passed 31 tests in 60.57s. A final focused input restriction and explicit fixture guard were then added; current full rerun and expanded Browser verification are pending. Types and build are being checked. No PR, CI, external review or completion of read-only hosting/tombstone controls is claimed at this checkpoint.
- The final 31-test run passed in 60.81s. Public result/audit disagreement now has a dedicated failure assertion; corrupted archives fail in the native verifier and invalid signatures fail before fetching an audit. Types and build passed. Live observer check/export/comparison succeeded again. After authorized E2E fixture cleanup removed the previously known poll, the live observer returned `PUBLIC_RESOURCE_MISSING` with exit 1 and retained its state byte-for-byte (`a9e8a219d9cb2ce3ab22d420979912b5662cb8b7486344fc45e6a7fd6c119efe`). This is missing-resource detection, not a claim of malicious deletion.
- Browser run `81f275401c` passed creation, review and all three encrypted answers but the agent resumed the duplicate-vote stage after its fixed 19:41 UTC deadline. The correctly closed page had no participation link, so the run failed. The deadline was not changed and this run is not counted as passed. A fresh run is required for complete Browser evidence.
- Fresh Browser run `3e69dc6d08` passed all 14 stages / 37 assertions with coordinator exit 0 at 19:49:32 UTC. Initial and final source digest both equal `60f66bccb46b72e18bc239f0eeae041e714e15a66256b74c94a67086d734b03a`. This includes the observer trust-center disclosure, complete voting journey, anonymous JSON/CSV/SVG/audit exports and provider failure/recovery controls. The unchanged downloaded archive independently passed offline Belenios verification: three ballots, closed status and `[[1,1,1]]`. Safe evidence is in `artifacts/e2e/`. Types, build, public-artifact hygiene and diff checks passed. Self-review found no additional required source changes; actual PR CI remains the merge gate.
- Observer [PR #47](https://github.com/Lontkovsky/vidkrytyi-holos/pull/47) merged as `e719ccb` after [actual CI success](https://github.com/Lontkovsky/vidkrytyi-holos/actions/runs/34056536033/job/101549361557), 3m37s. Issue #27 closed; no independent audit or completion of lawful withdrawal/read-only restoration is implied.

## Trustee proof intake and adversarial finality

- Audited #25, the original Belenios share verification and the application path from final archive to share storage and native result. A private synthetic reproduction created three encrypted votes, closed at the fixed deadline, submitted a malformed but hash-consistent share, then the genuine share. The API returned 200/recorded for the malformed share, 409/SHARE_ALREADY_RECORDED for its valid replacement, and 422/REFERENCE_REJECTED for tally. The test cleaned up only its own poll. The cause is confirmed: intake checked two-line structure, hash and claimed owner but deferred proof validation until after irreversible share storage.
- The upstream server checks each share using its native `E.check_factor` before persistence. The upstream CLI's generic election verification does not check partial decryption proofs until a result exists, so simply adding an event and calling `election verify` is insufficient. The planned minimal bridge calls the unchanged original library function, retains the native formats and verifies the complete archive first. No replacement cryptographic implementation or weak-error-based validation is introduced.
- The focused regression first failed with 200 instead of 422 for a corrupted proof. After native intake verification, that assertion and the genuine-share assertions passed; the later test tally failed because the test generated a share outside the CLI but did not persist its randomized proof bytes in the operator cache. The test now preserves its own share before delivery, matching the actual CLI. The focused service scenario passed in 50.43s without changing the immutable-share contract.
- The rebuilt native adapter and bootstrap passed. Twenty-one native reference checks passed in 38.173s, including all three permitted two-of-three quorums with the mandatory trustee, invalid proof/owner/hash rejection and early/late intake refusal. Seven focused trustee cases passed in 58.53s. The subsequent changes expand real service suppression checks to zero/one/two ballots, verify an above-threshold unanimous result, and disclose its privacy limit in the UI. Types/build passed; the final full run and Browser validation are pending.
- The expanded full run passed 39 of 40 tests; the management tally connection closed under concurrent native load. There was no service restart during that request. A separate test using the actual application factory and an eleven-second handler reproduced the same UND_ERR_SOCKET at 10.064s. Inspection of the pinned Fastify implementation confirmed that connectionTimeout sets the socket inactivity deadline, while requestTimeout limits incoming request reception. The application socket deadline now matches its existing 100-second request/client window instead of aborting valid native work after ten seconds. No automatic retry was added; request/body limits and native process limits remain in force. The transport and full suites are being rerun.
- The transport regression passed in 11.20s. The final full run passed all 41 tests / seven files in 122.51s, including the actual API tally under concurrent native work and all three suppressed group sizes. The sanitized passing report is in `artifacts/services/tests.json`. Types, build, smoke and public-artifact scan passed. The updated privacy explanation was inspected in the existing Browser at 390px; document/scroll widths both equal 390 and the temporary viewport override was reset.
- Browser run `9b10c9328d` passed creation, review and all three encrypted votes. A batched tool call exceeded its 60-second limit before the duplicate stage completed and reset the tool kernel. Reacquiring the existing Browser then reported that the Mac was locked and automatic unlocking failed. The user was asked to unlock it; no other browser was substituted. The coordinator was explicitly interrupted, preserving a failed/incomplete report and resetting the synthetic providers. This is not passing end-to-end evidence; a fresh complete run is required after the same Browser becomes available.
